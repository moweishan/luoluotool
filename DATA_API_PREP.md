# DATA_API_PREP.md — 外部服务 / API / 密钥 / 本地资源准备清单

> 结论先行：**本项目不需要任何外部 API、密钥、账号注册、云服务或网络请求。**
> 你不需要注册任何开发者平台，不需要申请 token，程序 100% 离线运行。
> 需要准备的全是**本地资源**，清单见下。
>
> 注（2026-09-15 政策变更）：「禁止任何联网代码」的限制已按用户要求取消（见 `PROJECT_SPEC.md` §4.1），
> 但本项目仍**不依赖**任何外部服务；且联网功能不得上传日志/配置/截图等本地数据，须经用户逐项授权。

---

## 1. 不需要准备的东西（明确排除）

| 类别 | 是否需要 | 说明 |
|---|---|---|
| 外部 API（LLM、语音、云函数等） | ❌ 不需要 | 无任何网络调用 |
| API Key / Token / 密钥 | ❌ 不需要 | 程序不持有任何密钥 |
| 第三方账号注册 | ❌ 不需要 | 不接入任何平台 |
| 数据库 / 云端存储 | ❌ 不需要 | 配置和日志都是本地文件 |
| 定时任务服务 | ❌ 不需要 | 无后台常驻调度 |
| 游戏官方 API / 开发者资格 | ❌ 不需要 | 本工具不访问游戏服务器数据 |

## 2. 本地资源清单

### 2.1 第一阶段必须准备（Phase 0 就要有）

| 资源 | 要求 | 检查方式 |
|---|---|---|
| Windows 系统 | Windows 10/11 x64 | `winver` |
| Python | 3.11 或 3.12（x64），确保 `python --version` 正常 | `python --version` |
| 磁盘空间 | ≥ 2 GB（虚拟环境 + 依赖 + 后续构建产物） | 资源管理器 |
| 代码编辑器 | 能打开本文件夹的 AI 代码编辑器（Codex / Cursor / Claude Code / Windsurf） | 直接打开本目录 |
| Git（可选但推荐） | 用于按阶段提交、回退 | `git --version` |

### 2.2 后期阶段必须准备（开始自动化前）

| 资源 | 要求 | 对应阶段 |
|---|---|---|
| 游戏客户端 | 《桃源深处有人家》已安装、可正常登录进入主界面 | Phase 4 起 |
| 游戏窗口模式 | **窗口化**运行（非全屏独占、非最小化到托盘） | Phase 4 起 |
| 显示缩放 | 建议 100%（缩放不是 100% 时，坐标映射需额外处理，MVP 不保证） | Phase 5 |
| 分辨率 | 单显示器；多显示器/超宽屏不保证 | Phase 5 |
| 测试小号（可选） | 若担心封号风险，先用小号验证脚本 | Phase 5 |
| 操作素材 | 用 Phase 4 的诊断截图工具，把要点击的按钮截图保存到 `assets/anchors/` | Phase 5（仅当启用图像匹配时） |

### 2.3 打包发布阶段必须准备（Phase 7）

| 资源 | 要求 |
|---|---|
| 干净测试机 | 一台**未安装 Python** 的 Windows 10/11 机器或虚拟机，用于验证 exe |
| 杀软白名单 | PyInstaller 产物可能被杀毒软件误报；正式使用前需在杀软中加白名单 |
| 图标 `icon.ico` | 256×256 及以下多尺寸 ICO 文件，放入 `packaging/` |
| 版本信息 | 应用名、版本号、版权说明，写入 `packaging/version_info.txt` |

## 3. 程序运行时自动生成的本地文件（无需手动准备）

| 文件/目录 | 内容 | 是否入库 |
|---|---|---|
| `user_data/config.json` | 用户实际配置（首次启动自动从默认值生成） | ❌ git 忽略 |
| `user_data/config.example.json` | 配置样例模板 | ✅ 入库 |
| `logs/luoluotool.log` | 滚动运行日志 | ❌ git 忽略 |
| `user_data/debug/*.png` | 诊断截图（窗口/锚点） | ❌ git 忽略 |
| `assets/anchors/*.png` | 图像匹配锚点 | ❌ git 忽略（个人素材） |

## 4. 隐私与安全提示（必读）

- 程序**不联网、不收集、不上传**任何数据；日志与截图只留在本机。
- 日志中可能包含坐标、窗口标题等操作信息，**不要把 `logs/`、`user_data/` 打包发给别人**。
- 游戏账号密码只存在游戏客户端里，程序不会也不需要接触。
- 若更换电脑或重装系统，只需备份 `user_data/config.json` 即可恢复配置。
